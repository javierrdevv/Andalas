# AGENTS.md

This repository is empty (no code, manifests, or config yet). No verified commands or conventions exist to document.

When the project takes shape, replace this file with:
- exact build/test/lint/typecheck commands
- entrypoints and package boundaries
- repo-specific conventions that differ from tool defaults
